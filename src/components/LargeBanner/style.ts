import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  wrapperBanner: {
    backgroundColor: '#333',
    alignItems: 'center',
    justifyContent: 'center',
    width: '100%',
    height: 110,
    marginVertical: 30,
  },
});
