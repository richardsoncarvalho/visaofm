import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  wrapperBanner: {
    alignItems: 'center',
    justifyContent: 'center',
    width: '100%',
    height: 110,
    marginVertical: 30,
  },
});
